{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "babel-jest": "^29.7.0",
    "ethers": "^6.8.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "^5.0.1",
    "web3": "^4.2.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "start1": "react-scripts --openssl-legacy-provider start",
    "start2": "NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "start3": "NODE_OPTIONS='--openssl-legacy-provider --trace-deprecation' react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start-v18": "react-scripts --openssl-legacy-provider start",
    "build-v18": "react-scripts --openssl-legacy-provider build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version",
      ">0.3%",
      "not ie 11",
      "not dead",
      "not op_mini all"
    ]
  }
}